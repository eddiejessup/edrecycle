<!DOCTYPE html>
<html lang="en">
    <head>
        {% block head %}
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width,initial-scale=1, maximum-scale=1">
        <meta name="author" content="Elliot Marsden">
        <meta name="description" content="A-Z of Edinburgh recycling locations">
        <meta name="keywords" content="Recycling, Calendars, Edinburgh">

        <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
        <link rel="stylesheet" href="{{ url_for('static', filename='custom-theme/jquery-ui-1.10.0.custom.css') }}">
        <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
        <title>{% block title %}{% endblock %}</title>
        {% endblock %}

        {% block scripts %}
            <script src="//code.jquery.com/jquery-2.1.1.min.js"></script>
            <script src="{{ url_for('static', filename='custom-theme/jquery-ui-1.9.2.custom.min.js') }}"></script>
            <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
            <!-- So that javascript knows where my site is -->
            <script type=text/javascript>
              $SCRIPT_ROOT = {{ request.script_root|tojson|safe }};
            </script>
        {% endblock %}
    </head>
    <body>
        {% block navbar %}
            <nav class="navbar navbar-default" role="navigation">
                <div class="container">
                    <a class="navbar-brand" href="{{ url_for('search') }}">EdRecycle</a>
                    <ul class="nav navbar-nav">
                        <li><a href="{{ url_for('help') }}">Help</a></li>
                        <li><a href="{{ url_for('about') }}">About</a></li>
                    </ul>
                </div>
            </nav>
        {% endblock %}

        <div class="container">
            <header id="notice">Note: Unfortunately, Edinburgh Council stopped posting the collection dates in a form that is easily computer-readable at the end of 2014, so these are no longer available. Sorry!</header>
        </div>

        {% block content %}{% endblock %}
    </body>
</html>
